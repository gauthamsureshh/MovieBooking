import "../Styles/Ticket.css"
import React,{useContext} from "react"
function Ticket(){
    const ticket=useContext(ticketContext)
    // const date = (props.date).toLocaleDateString('en-GB', { day: '2-digit', month: 'short' });
    return(
       <p>dd</p>
    )
}
export default Ticket